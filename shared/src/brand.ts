/**
 * Brand tokens ของ THNP ClinicQ
 *
 * ค่าสีทั้งหมดสุ่มวัดมาจากไฟล์โลโก้จริง (frontend/img — โลโก้ทองบนพื้น navy)
 * ไม่ใช่สีที่เดาเอง: จุดยึด 4 ค่าที่วัดได้ถูกทำเครื่องหมาย [วัดจากโลโก้] ไว้
 *
 * ที่นี่คือแหล่งความจริงแหล่งเดียวของสีแบรนด์ — frontend ใช้ผ่าน Tailwind theme
 * และ backend ใช้ตอนประกอบ LINE Flex Message เพื่อให้ข้อความในแชทเป็นแบรนด์เดียวกับหน้าจอ
 */

/** พื้นหลังหลัก — น้ำเงินกรมท่าจากพื้นโลโก้ */
export const NAVY = {
  950: '#060D15', // [วัดจากโลโก้] จุดที่เข้มที่สุดของพื้นหลัง
  900: '#0B1523',
  800: '#0E1B2C',
  700: '#142132', // [วัดจากโลโก้] ค่าเฉลี่ยพื้นหลัง = navy หลักของแบรนด์
  600: '#1C2D42',
  500: '#2A3E56',
  400: '#3D5470',
} as const;

/** สีหลักของแบรนด์ — ทองจากตัวอักษรและสัญลักษณ์ในโลโก้ */
export const GOLD = {
  100: '#FCF1C4', // [วัดจากโลโก้] ไฮไลต์ที่สว่างที่สุดบนเส้นทอง
  200: '#F0DFAE',
  300: '#E3CD9B',
  400: '#D3BC88', // [วัดจากโลโก้] ค่าเฉลี่ยของทองทั้งโลโก้ = gold หลักของแบรนด์
  500: '#C0A472',
  600: '#A98C5C',
  700: '#967A4B', // [วัดจากโลโก้] เงาที่เข้มที่สุดของเส้นทอง
} as const;

/** สีแบรนด์ที่ใช้บ่อยที่สุด — อ้างอิงตัวนี้เวลาต้องการ "สีแบรนด์" ลอย ๆ */
export const BRAND = {
  gold: GOLD[400],
  goldBright: GOLD[100],
  goldDeep: GOLD[700],
  navy: NAVY[700],
  navyDeep: NAVY[950],
  /** ไล่เฉดทองแบบเดียวกับตัวอักษรในโลโก้ ใช้กับหัวข้อใหญ่หรือปุ่มหลัก */
  goldGradient: `linear-gradient(135deg, ${GOLD[600]} 0%, ${GOLD[100]} 45%, ${GOLD[500]} 100%)`,
} as const;

/**
 * สีประจำสถานะนัด — ใช้ทั้งบนบอร์ดคิวและใน Flex Message
 * ทุกสีเลือกให้อ่านออกบนพื้น navy และไม่ตีกับทอง
 */
export const STATUS_COLOR = {
  BOOKED: '#E0B33C', // เหลืองอำพัน — จองแล้ว รอลูกค้ายืนยัน
  CONFIRMED: '#3FB984', // เขียว — ลูกค้ากดยืนยันแล้ว
  RESCHEDULE_REQUESTED: '#E8863C', // ส้ม — ขอเลื่อน รอร้านติดต่อกลับ
  CANCELLED: '#6B7A8F', // เทาน้ำเงิน — ยกเลิก
  NO_SHOW: '#D9534F', // แดง — ไม่มาตามนัด
  COMPLETED: '#4A6FA5', // น้ำเงิน — รับบริการเรียบร้อย
} as const;

/** ข้อมูลร้านสำหรับ demo — ของจริงย้ายไปตั้งค่าใน DB ตอนส่งมอบลูกค้า */
export const BRAND_INFO = {
  name: 'THNP Clinic',
  fullName: 'THNP CLINIC — Aesthetics & Wellness',
  productName: 'ClinicQ',
  tagline: 'ระบบนัดหมายและดูแลลูกค้าผ่าน LINE',
  timezone: 'Asia/Bangkok',
} as const;
